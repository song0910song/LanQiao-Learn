# 算法技巧与模板

## 1. 最大公约数、同余原理

### 最大公约数

#### Python代码

```python
# 方法1
def myGcd(a, b):
    return a if b == 0 else myGcd(b, a%b)

# 最小公倍数
def lcm(a, b):
    return a // myGcd(a, b) * b
```

#### 题目：第n个神奇的数字

> 一个正整数如果能被 `a` 或 `b` 整除，那么它是神奇的。
>
> 给定三个整数 `n` , `a` , `b` ，返回第 `n` 个神奇的数字。因为答案可能很大，所以返回答案是对 $1e9 + 7$ **取模** 后的值。

- 分析：第n个神奇数一定在[1, n*min(a, b)]范围内。使用二分法选定中点m，且使用公式$\frac{m}{a}+\frac{m}{b}-\frac{m}{lcm(a, b)}$得到第k个神奇数，若k>=n, ans = k, r = m - 1; 若k < n, r = m + 1。

- ```Python
  from math import lcm
  
  def nthMagicalNumber(n, a, b):
      r = n * min(a, b)
      l = 0
      ans = 0
  
      while l <= r:
          m = (l + r) // 2
          if (m // a + m // b - m // lcm(a, b) >= n):
              ans = m
              r = m - 1
          else:
              l = m + 1
      return int(ans % (1e9 + 7))
  
  print(nthMagicalNumber(1000000000, 40000, 40000))
  ```

### 同余原理

- 方法：加法、乘法每一步计算后直接取模，即$((a + b)+(c+d)) \% m = (a + b)\%m + (c + d) \% m$，减法则为$(a-b+m)\%m$

- 中间过程计算结果较大时可用

- ```python
  # 同余原理举例
  
  from random import randint
  
  
  # 计算 ((a + b) * (c - d) + (a * c - b * d)) % mod的结果
  def f1(a, b, c, d, mod):
      total = ((a + b) * (c - d) + (a * c - b * d)) % mod
      return int(total) if total >= 0 else int(total + mod)
  
  
  def f2(a, b, c, d, mod):
      o1 = a % mod  # o1
      o2 = b % mod  # o2
      o3 = c % mod  # o3
      o4 = d % mod  # o4
      o5 = (o1 + o2) % mod  # (a + b)
      o6 = (o3 - o4 + mod) % mod  # (c - d)
      o7 = (o1 * o3) % mod  # (a * c)
      o8 = (o2 * o4) % mod  # (b * d)
      o9 = (o5 * o6) % mod  # (a + b) * (c - d)
      o10 = (o7 - o8 + mod) % mod  # (a * c - b * d) % mod
      ans = (o9 + o10) % mod
  
      return ans
  
  
  def random():
      return randint(10**6, 10**8)
  
  
  a = random()
  b = random()
  c = random()
  d = random()
  MOD = 10**8 + 7
  
  print("测试开始")
  print("=" * 20)
  print(f"a={a}\nb={b}\nc={c}\nd={d}")
  print()
  print(f1(a, b, c, d, MOD))
  print(f2(a, b, c, d, MOD))
  
  ```




## 构建前缀信息的技巧（枚举技巧）

### 1.构建前缀和数组

快速解决子数组范围求和的问题

 ```markdown
 数组：[3, 2, -5, 6, 2, 1]
        ↑  ↑  ↑  ↑  ↑  ↑
        0  1  2  3  4  5
 ```

先在前缀和数组中添个0，--> [**0**, ]，**为了数组越界的情况**
前一个数的和为：3，--> [0, 3, ]
前两个数的和为：3 + 2，--> [0, 3, 5]
前三个数的和为：5 + (-5) --> [0, 3, 5, 0]
....
**前n个数的和为：sum[i] = sum[i-1] + arr[i]**

求[l, r]范围上的和，即sum(l, r)，公式为：
$$
\large{sum(l, r) = sum(0, r) - sum(0, l-1)} = sum(0, r+1) - sum(0, l)
$$

```python
# 子数组求和问题 -- 模版

def NumArray(arr: list[int]) -> list[int]:
    sum_arr = [0] * (len(arr) + 1)

    for i in range(1, len(arr) + 1):
        sum_arr[i] = sum_arr[i - 1] + arr[i - 1]
    return sum_arr

def sum_range(sum_arr: list[int], i: int, j: int) -> int:
    return sum_arr[j+1] - sum_arr[i]

t = [3, 2, -5, 6, 2, 1]
print(NumArray(t))
print(sum_range(NumArray(t), 2, 3))
```

### 2. 构建 *前缀和*  最早出现的位置

> 题目描述：
> 给定一个数组 nums 和一个目标值 k，找到和等于 k 的最长**子数组**长度。如果不存在任意一个符合要求的子数组，则返回 0。
>
> 注意:
>  nums 数组的总和是一定在 32 位有符号整数范围之内的。
>
> 示例 1:
>
> 输入: nums = [1, -1, 5, -2, 3], k = 3
> 输出: 4 
> 解释: 子数组 [1, -1, 5, -2] 和等于 3，且长度最长。
> 示例 2:
>
> 输入: nums = [-2, -1, 2, 1], k = 1
> 输出: 2 
> 解释: 子数组 [-1, 2] 和等于 1，且长度最长。

- python代码

- ```python
  # 构建前缀和最早出现的位置
  
  def longest_subarray_sum_equal_is_aim(arr, aim):
      # key: 前缀和 
      # value: 前缀和最早出现的位置
      sum_map = {0:-1}
      ans = 0
      # 前缀和
      sum_arr = 0
  
      for i in range(len(arr)):
          sum_arr += arr[i]
          if sum_map.get(sum_arr - aim) is not None:
              ans = max(ans, i - sum_map[sum_arr - aim])
          if sum_arr not in sum_map:
              sum_map[sum_arr] = i
  
      return ans
  
  t = [3, 2, -5, 6, 2, 1]
  print(longest_subarray_sum_equal_is_aim(t, 5))
  ```

  对于一个数组**nums**，构建其前缀和**sum_arr**，令**sum_arr - aim**，可得到前缀和最早出现的位置idx。令i-dix可得到和为aim数组的长度。



### 3. 指定子数组累加和出现的次数

 与**题2**相似，python代码如下：

```python
# 给定一个数组arr和一个整数aim，返回arr中有多少个子数组的累加和等于aim

from random import randint

def Count_EqualIsAim(arr: list[int], aim: int):
    # key: 前缀和 
    # value: 前缀和出现的次数
    sum_map = {0:1}
    ans = 0
    # 前缀和
    sum_arr = 0

    for i in range(len(arr)):
        sum_arr += arr[i]
        ans += sum_map.get(sum_arr - aim, 0)
        sum_map[sum_arr] = sum_map.get(sum_arr, 0) + 1

    return ans

def Count_EqualIsAim2(arr: list[int], aim: int):
    ans = 0
    len_arr = len(arr)

    '''
        例
        [ 3, 2, -5, 6, 2, 1 ]
        
        3
        i,j
        sum_arr = 3

        3, 2
        i  j
        sum_arr = 3 + 2 = 5

        3, 2, -5
        i      j
        sum_arr = 3 + 2 - 5 = 0

        ......

    '''

    for i in range(len_arr):
        for j in range(i, len_arr):
            sum_arr = 0
            for k in range(i, j+1):
                sum_arr += arr[k]

            if sum_arr == aim:
                ans += 1

    return ans


def validate():
    for i in range(1000):
        arr = [randint(-100, 100) for _ in range(randint(1, 20))]
        aim = randint(-100, 100)
        print("arr is: ", arr)

        m1 = Count_EqualIsAim(arr, aim)
        m2 = Count_EqualIsAim2(arr, aim)
        if Count_EqualIsAim(arr, aim) != Count_EqualIsAim2(arr, aim):
            print("Missive mistake!!!")
            print(m1)
            print(m2)
            break
    else:
        print('the test is pass!!!')

validate()
```



### 4. 子数组中正负一样长的最长数组

一个巧妙的转换方法，**把数组中小于0的变为-1，数组中大于0的变为1❤️**，其余步骤与题2相同。

```python
# 正负一样长的最长子数组

def getMaxLength(nums):

    sum_arr = 0
    # key: 前缀和
    # value: 前缀和最早出现的位置
    sum_map = {0: -1}
    # 答案
    ans = 0

    for i in range(len(nums)):
        sum_arr += nums[i]
        # print(sum_arr)
        # 如果前缀和之前出现过，说明之前的前缀和到现在的前缀和之间的子数组的累加和为0
        if sum_map.get(sum_arr) is not None:
            ans = max(ans, i - sum_map[sum_arr])
        else:
            sum_map[sum_arr] = i
    return ans

def getMaxLength2(nums):
    # 对数器
    ans = 0

    for i in range(len(nums)):
        for j in range(i, len(nums)):
            sum_arr = 0
            for k in range(i, j+1):
                sum_arr += nums[k]
            if sum_arr == 0:
                ans = max(ans, j - i + 1)
    return ans
            


from random import randint

def validate():
    # 验证器
    for _ in range(1000):
        arr = [randint(-1, 1) for _ in range(randint(1, 20))]
        if getMaxLength(arr) != getMaxLength2(arr):
            print("Oops!")
            print(arr)
            print(getMaxLength(arr))
            print(getMaxLength2(arr))
            break
    else:
        print("Nice!")

validate()
```



### 5. 移除最短子数组长度，使得剩余元素的累加和能被p整除

* 方法：

  > 1. 整体累加 % p = mod
  > 2. 构建前缀和，并 % p = cur
  > 3. 若cur >= mod，则使用cur - mod；若cur < mod，则使用(cur + p - mod)。
  > 4. 如果找到，则记录最短长度。
  > 5. 使用字典{前缀和%p的余数: 最晚出现的位置}更新。

```python
import math

def f(arr: list[int], p: int):
    mod = sum(arr) % p

    if mod == 0:
        return 0
    
    sum_map = {0: -1}
    ans = math.inf
    find = 0
    cur = 0

    for i in range(len(arr)):
        # 0...i这部分的余数
        cur = (cur + arr[i]) % p
        find = cur - mod if cur >= mod else cur - mod + p

        # 如果之前出现过find这个余数，说明0...sum_map[find]这部分的余数是find，那么sum_map[find]+1...i这部分的余数就是mod
        if find in sum_map:
            ans = min(ans, i - sum_map[find])
        sum_map[cur] = i
    
    return ans if ans != len(arr) else -1

t = [3, 1, 4, 2]
p = 6
print(f(t, p))

```



## 一维差分和等差差分

### 一维差分

- 有一个数列arr，初始值都为0，现有多个操作：2~5，+3；1~6，-2；4~7，+5 ...... [L, R]，+n，求执行完这些操作后，数列arr的值。

- 方法：1. arr[2] +=3，arr[5+1] -=3；arr[1] -=2，arr[6+1] +=2；arr[L]+=n，arr[R+1]-=n。

  ```python
  def diff_array(arr: list[list[int]], n: int):
      # 构建差分数组
      diff = [0 for i in range(n + 1)]
  
      for row in arr:
          diff[row[0]] += row[2]
          diff[row[1] + 1] -= row[2]
  
      # 构建前缀和
      for i in range(1, n+1):
          diff[i] += diff[i-1]
      
      return diff[:-1]
  
  print(diff_array([[2, 5, 3], [1, 6, -2], [4, 7, 5]], 9))
  
  ```




### 等差数列差分

- 有一个数列arr，初始值都为0，接下来一共有m个操作。每次操作，在l~r范围上依次加上首项为s、末项为e、公差为d的等差数列。求最终数列arr的值。
- 方法：arr[l] += s; arr[l+1] += d - s; arr[r+1] -= d + e; arr[r+2] += e; 最后求两次前缀和
- 原理：

![image-20260214215753806](F:\lanqiaoPython\t1.png)



### 二维前缀和

累加：**左 + 上 - 左上 + 自己**。

` sum[i][j]: 代表左上角(0, 0)到右下角(i, j)这个范围的累加和 `
$$
{\large sum[i][j] = sum[i][j-1] + sum[i-1][j] - sum[i-1]sum[j-1] + sum[i][j]}
$$

![](F:\lanqiaoPython\二维前缀和累加.jpg)

从(0, 0)到(i, j)的和可以表示为：以(i, j)为中心，+自己左侧的值+自己上方的值+自己-减去左上，即为从(0, 0)到(i, j)的和。

### 二维前缀和查询

**sum里面已经储存了(0, 0)到(i, j)的前缀和**，现在需要查询(a, b)到(c, d)的累加和。

<img src="F:\lanqiaoPython\二维前缀和查询.jpg" style="zoom:67%;" />

公式为：
$$
{\large sum[c][d] - sum[c][b-1] - sum[a-1][d] + sum[a-1][b-1]}
$$


## 单调栈

### 1.无重复的数组值使用单调值

要寻找数组值i，**左边比i小的最近的数组下标**，**右边比i小的最近的数组下标**，维护一个栈，依照**大压小**的原则进栈，若栈顶数组值j大于要压进的数组值k，栈顶的数组值k弹出，左边比k小的最近的数组下标为栈里的下一个数组值下标，右边比k小的最近的数组下标为j的下标。

![image-20260317094434559](F:\lanqiaoPython\栈\大压小.png)

**找比自己大的数组值下标类似**

### 2. 有重复值的数组值

过程与`1.无重复的数组值使用单调值`类似。

![image-20260317095018959](F:\lanqiaoPython\栈\有重复值.png)

### 3. 单调栈经典模板

> 牛客：[单调栈](https://ac.nowcoder.com/acm/problem/217405)

```python
#
# 代码中的类名、方法名、参数名已经指定，请勿修改，直接返回方法规定的值即可
#
# 
# @param nums int整型一维数组 
# @return int整型二维数组
#
class Solution:
    def foundMonotoneStack(self , nums: List[int]) -> List[List[int]]:
        # write code her
        stack = []
        n = len(nums)
        # ans[i][0]表示左边第一个比arr[i]小的数的索引，ans[i][1]表示右边第一个比arr[i]小的数的索引
        ans = [[-1 for _ in range(2)] for _ in range(n)]
        for i in range(n):
        # 维护一个单调递增的栈，栈顶元素对应的值最小
            while stack and nums[stack[-1]] >= nums[i]:
                cur = stack.pop()
                ans[cur][1] = i
                ans[cur][0] = stack[-1] if stack else -1
            stack.append(i)

        # 处理剩余的元素
        while stack:
            cur = stack.pop()
            ans[cur][1] = -1
            ans[cur][0] = stack[-1] if stack else -1

        # 处理重复元素
        for i in range(n-2, -1, -1):
            if ans[i][1] != -1 and nums[ans[i][1]] == nums[i]:
                ans[i][1] = ans[ans[i][1]][1]
                
        return ans
```

## 并查集

方法步骤：

- 初始每个元素都拥有自己的集合，在自己的集合中只有自己这个元素。
- 函数$find(i)$：查找$i$所在集合的**代表**元素，代表元素来代表$i$所在的集合
- 函数$isSameSet(a, b)$：判断a和b在不在一个集合里。
- 函数$union(a, b)$：a,b两个集合合并成一个集合。

